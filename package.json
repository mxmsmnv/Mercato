{
  "name": "mercato-acceptance",
  "private": true,
  "version": "1.3.0",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:live": "playwright test --grep @live"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.55.0"
  }
}
